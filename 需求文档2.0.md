## 任务
完善功能

###

「数字栏」「配置栏」这个字去掉，简洁一点

数字由6行扩展到9行

染色阈值不用手动指定，默认左（小）红右（大）蓝 ，染色阈值那个数字和阈值选择栏同行。

删除开始染色按钮，改为调整阈值或者点击当前阈值或者更改比较方式的时候自动执行一次染色

数字范围为0-9以及空，空代表没有输入。

简化编辑格子的操作，在编辑页面，数字0-9 空直接都做完一个小按钮显示出来，方便选择。颜色只有 红 蓝 清空三种颜色图标，选择颜色是直接选择整个格子的田字四分颜色，不再单独选择。锁定按钮也弄成一个统一样式的按钮。删除保存，取消按钮，直接应用用户修改的选择，点击编辑框外退出这一格的编译。

记录输入的内容，不要每次打开就全部清空，全体清0改成上移一行，将所有数字行状态上移一行，包括数字，颜色，锁定状态等，最底下也就是最后一行为空。
